import { VFC } from 'react';
import SampleStyledDiv from '../../styles/sample-styles';

const SampleNestedIndex: VFC = () => (
  <SampleStyledDiv>
    <p>SampleNestedLayout / NestedTestIndex</p>
  </SampleStyledDiv>
);

export default SampleNestedIndex;
