import { VFC } from 'react';
import SampleStyledDiv from '../../styles/sample-styles';

const SampleRoot: VFC = () => (
  <SampleStyledDiv>
    <p>SampleRoot</p>
  </SampleStyledDiv>
);

export default SampleRoot;
